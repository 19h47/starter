/* –––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––
 * App
 * –––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––– */

define( ["jquery", "home", "page", "post", "posts", "menu", "functions"], function (jQuery, Home, Page, Post, Posts, Menu, functions) {

var App = {
	/* -------------------------------------------------------------------------
	 * Constants
	 * ---------------------------------------------------------------------- */
	__NAMESPACE__: '.app',

	/* -------------------------------------------------------------------------
	 * Private
	 * ---------------------------------------------------------------------- */
	
	/* -------------------------------------------------------------------------
	 * Public
	 * ---------------------------------------------------------------------- */

	/* -------------------------------------------------------------------------
	 * DOM public elements
	 * ---------------------------------------------------------------------- */
	$body: null,

	/* -------------------------------------------------------------------------
	 * DOM private elements
	 * ---------------------------------------------------------------------- */

	// Functions
	init: function(){
		// console.log('App.init');


		Home.init();
		Page.init();
		Post.init();
		Posts.init();
		Menu.init();

		//Please note, the DOM should be ready
		Barba.Pjax.start();
		Barba.Prefetch.init();
	}
};

return App;

});